# Bindings

This directory will contain **bindings** that connect:

- concept nodes ↔ concept families
- concept nodes ↔ axes of variation
- concept nodes ↔ acquisition motifs

Decision for v0.1: keep bindings in **separate files** (rather than embedded in concept nodes).

